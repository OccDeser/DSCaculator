import React from 'react';
import ReactDOM from 'react-dom';
import 'antd/dist/antd.min.css'
import DSCApp from './DSCApp';

ReactDOM.render(
  <React.StrictMode>
    <DSCApp />
  </React.StrictMode>,
  document.getElementById('root')
);
